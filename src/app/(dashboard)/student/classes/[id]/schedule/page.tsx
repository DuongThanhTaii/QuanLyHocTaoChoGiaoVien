import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
export default async function Page({ params }: { params: Promise<{id:string}> }) { const {id}=await params; const {createClient}=require('@supabase/supabase-js'); const admin=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.SUPABASE_SERVICE_ROLE_KEY!); const {data}=await admin.from('class_sessions').select('session_date,start_time,end_time,title').eq('class_id',id).order('session_date'); return <Card><CardHeader><CardTitle>Thời khóa biểu</CardTitle></CardHeader><CardContent>{data?.length ? <ul className="space-y-3">{data.map((s:any)=><li key={`${s.session_date}${s.start_time}`}>{new Date(s.session_date).toLocaleDateString('vi-VN')} · {s.start_time}–{s.end_time} · {s.title||'Buổi học'}</li>)}</ul>:'Chưa có lịch học.'}</CardContent></Card> }

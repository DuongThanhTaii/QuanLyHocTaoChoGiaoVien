'use client';
import Link from 'next/link'; import { useSearchParams } from 'next/navigation'; import { cn } from '@/lib/utils';
export function StudentClassTabs({ classId }: { classId: string }) { const params = useSearchParams(); const active = params.get('tab') || 'overview'; const tabs = [['overview','Tổng quan'],['students','Học sinh'],['schedule','Lịch học'],['attendance','Điểm danh'],['lessons','Bài giảng']]; return <div className="overflow-x-auto border-b border-zinc-200"><nav className="-mb-px flex min-w-max space-x-8">{tabs.map(([key,name]) => <Link key={key} href={`/student/classes/${classId}?tab=${key}`} className={cn(active === key ? 'border-zinc-900 text-zinc-900' : 'border-transparent text-zinc-500 hover:text-zinc-700', 'whitespace-nowrap border-b-2 px-1 py-4 text-sm font-medium')}>{name}</Link>)}</nav></div>; }
